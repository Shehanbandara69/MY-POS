# Streamlit Secrets Configuration
# Place this file at: .streamlit/secrets.toml

# Database Connection
[connections.sqlite]
url = "sqlite:///pos_system.db"

# User Credentials
[user_credentials]
admin_password = "admin123"
cashier_pin = "0000"

# Optional: Tax Configuration
[settings]
tax_rate = 0.0  # Set to 0.18 for 18% tax, etc.
currency_symbol = "Rs"
